/**
 * @deprecated Replaced by the CMS-driven `MobileNav` (Phase 5). This module is
 * retained only as a thin re-export so any lingering import keeps resolving;
 * the hard-coded mobile menu is gone. Import `MobileNav` directly for new code.
 */
export {default} from './MobileNav'
